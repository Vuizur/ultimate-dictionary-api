* Endpoint that allows creation of a dictionary
* Create a Translation response class
    - entries that have a form that fits
* Normalization of Russian entries (remove accent)
* Create DTO for translation endpoint
* Don't add translations to response if source and target lang are the same
* Search for cyrillic translations from latin languages in Russian wiktionary, maybe open bug if large problem
* Random word API: Add a random float between 0 and 1 to each word in the dictionary, and index this. Then, when a random word is requested, get a random float between 0 and 1 and return the word with the closest float.