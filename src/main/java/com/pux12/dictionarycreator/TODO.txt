* Endpoint that allows creation of a dictionary
* Endpoint that allows to enter base lang + target lang and display results
* Create a Translation response class
    - (DONE) contains all translations in translation boxes
    - contains all glosses + metainfo if target lang is wiktionary
    - looks in all wiktionaries for entries that have target language translation
    - entries that have a form that fits
* Normalization of Russian entries (remove accent)
* Create DTO for translation endpoint
* Don't add translations to response if source and target lang are the same
* Add translations from German Wiktionary
* Search for cyrillic translations from latin languages in Russian wiktionary